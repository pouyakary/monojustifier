This is the first paragraph that should be wrapped properly with justification across multiple lines to ensure separation.

This is the second paragraph which should also be wrapped and separated accordingly so the algorithm handles blank lines.
---
This is the first paragraph that should be
wrapped properly with justification across
multiple lines to ensure separation.

This  is the second paragraph which should
also be wrapped and separated  accordingly
so the algorithm handles blank lines.
